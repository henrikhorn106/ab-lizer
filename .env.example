# Environment Configuration for AB Lizer

# Flask Environment (development, testing, production)
FLASK_ENV=development

# Secret Key for Flask (change in production!)
SECRET_KEY=your-secret-key-here

# Database URL (optional, defaults to SQLite)
# For development:
# DEV_DATABASE_URL=sqlite:///data/database.db
# For production (example with PostgreSQL):
# DATABASE_URL=postgresql://user:password@localhost/ablizer
